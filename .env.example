# Copia in .env.local (mai nel repository) e compila quello che serve.

# Server TURN per le videochiamate (facoltativi, vedi src/data/calls.js).
VITE_TURN_URLS=
VITE_TURN_USERNAME=
VITE_TURN_CREDENTIAL=
